<p id="notice"><%= notice %></p>

<p>
  <b>Email:</b>
  <%= @user.email %>
</p>

<p>
  <b>Password digest:</b>
  <%= @user.password_digest %>
</p>
  
<p>
	<% @potential_colleges.compact.each do |college|  %>
  		<%= college.name %>
  	<% end %>
</p>

<p>
	<b>Your Activities:</b>
	<% @user.activities.each do |activity|  %>
  		<%= activity.activity_name %>
  	<% end %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
<%= link_to 'Potential_user', users_path(@potential_user) %>